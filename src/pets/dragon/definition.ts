import { createPetDefinition } from '../createPetDefinition';
import { dragonPersonality } from './personality';

export const dragonDefinition = createPetDefinition('dragon', 'Dragon', dragonPersonality);
