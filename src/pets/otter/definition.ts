import { createPetDefinition } from '../createPetDefinition';
import { otterPersonality } from './personality';

export const otterDefinition = createPetDefinition('otter', 'Otter', otterPersonality);
